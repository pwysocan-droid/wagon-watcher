# design-refs

User-supplied HTML mockups and design references for the watcher's user-facing
surfaces (notification embeds, weekly digest, future dashboard).

Drop files here. They inform implementation in build steps 5, 8, and any future
dashboard work. The aesthetic spec lives in PROJECT.md ("UI / Design" section);
files here are concrete references to match.
